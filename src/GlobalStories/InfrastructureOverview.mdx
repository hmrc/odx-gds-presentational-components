import { CheckBox } from '../Components/CheckBox';
import { Meta } from "@storybook/addon-docs"

<Meta title='Infrastructure/Overview' />

# Infrastructure

These components are used to support other components, but are not expected to be used indiviudally.
However, they are exposed for use in case they are needed for building custom components.